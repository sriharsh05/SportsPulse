import Matches from "../../components/liveMatches";

const Home = () => {
  return (
    <main className="flex flex-col">
      <section>
        <Matches />
      </section>
    </main>
  )
};

export default Home;